
  <h1 class="map">Calendar</h1>
  <div class="table-responsive">
    <table class="table table-striped table-condensed">

      <!---Body--> 
      <tbody>
        {{#each calendar}}
        <tr>
          <td>{{date}} <br> {{location}}<br>
         <a href={{calendarURL}}>Add to Google Calendar</a>

</td>
          <td style="background-color: #93c0bd;"><iframe width='100%' height='150px' frameBorder='0' src={{mapURL}}></iframe></td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
